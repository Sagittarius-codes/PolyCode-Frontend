/** Unified learn UI accent — use instead of per-chapter rainbow colors. */
export const LEARN_ACCENT =
  "var(--lesson-accent, var(--acid, #ffe566))";

/** W3 topic overview — follows active theme palette, not course brand color. */
export const THEME_OVERVIEW_ACCENT = "var(--accent, var(--acid, #818cf8))";
